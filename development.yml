version: '2'
services:
  php:
    build: ./build/php
    volumes:
      - ./app:/home/www-data:ro
    environment:
      APP_ENV: develop
    links:
      - ftp
  ftp:
    build: ./build/ftp
    volumes:
      - ./var/data:/data/z01/pastdata:ro
    privileged: true